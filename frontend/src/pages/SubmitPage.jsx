import PageShell from "./PageShell";

function SubmitPage() {
  return (
    <PageShell
      eyebrow="Contribution"
      title="Submit new entries into the marketplace stream."
      description="The submit route keeps the same visual rhythm so forms can slot in later without redesigning the shell."
    />
  );
}

export default SubmitPage;
