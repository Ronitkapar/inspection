import PageShell from "./PageShell";

function LoginPage() {
  return (
    <PageShell
      eyebrow="Access"
      title="Log in through the midnight greenhouse."
      description="The login route is ready for auth work later, with the shared theme and layout already in place."
    />
  );
}

export default LoginPage;
